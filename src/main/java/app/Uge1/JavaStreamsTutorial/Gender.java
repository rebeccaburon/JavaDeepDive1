package app.Uge1.JavaStreamsTutorial;

/**
 * An enum in Java is like a list of specific, unchangeable options that a variable can have.
 * For example, with Gender enum, a person's gender can only be MALE or FEMALE
 */
public enum Gender {
    MALE, FEMALE
}